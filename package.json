{
  "name": "ina219",
  "version": "0.0.1",
  "description": "INA219 current sensor driver bindings",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "debug-build": "node-gyp rebuild --debug && tsc",
    "configure": "node-gyp configure",
    "build": "node-gyp build && tsc",
    "rebuild": "node-gyp rebuild && tsc",
    "bear-build": "bear -- node-gyp build && tsc",
    "bear-rebuild": "bear -- node-gyp rebuild && tsc"
  },
  "keywords": [
    "INA219", "OpenI2C"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/debug": "^4.1.12",
    "@types/jest": "^29.5.14",
    "@types/node": "^22.15.18",
    "jest": "^29.7.0",
    "node-gyp": "^11.2.0",
    "ts-jest": "^29.3.2",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "bindings": "^1.5.0"
  }
}
